import { TouchableOpacity, View } from 'react-native';
import { CLSStyles } from "../styles/styles.js";

export default function CharactersDetailScreen({ navigation }) {
    return(
        <View style={CLSStyles.container}>
            
        </View>
    );
}